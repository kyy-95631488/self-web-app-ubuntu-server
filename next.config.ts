import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['alan-rfc-queen-supplements.trycloudflare.com'],
  devIndicators: false,
};

export default nextConfig;
